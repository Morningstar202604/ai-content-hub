# 后端
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
requests>=2.31
markdown>=3.5    # 知乎/B站/头条等富文本编辑器发布时 Markdown 转 HTML
mcp>=1.0         # MCP 官方 SDK（server/mcp_server.py 用，替代原手写 JSON-RPC）

# 浏览器自动化（反检测 fork，替代 playwright + 手写 stealth JS；
# 装完记得：python3 -m patchright install chromium；国内网络失败时
# 可复用预装 playwright chromium，core/browser.py 会自动探测）
patchright>=1.60

# 后端
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
requests>=2.31
markdown>=3.5    # 知乎/B站/头条等富文本编辑器发布时 Markdown 转 HTML
mcp>=1.0         # MCP 官方 SDK（server/mcp_server.py 用，替代原手写 JSON-RPC）

# 浏览器自动化（装完记得：playwright install chromium）
playwright>=1.48

# 工作流引擎（LangGraph，ARCHITECTURE.md ADR-001）
langgraph>=1.2
langgraph-checkpoint-sqlite>=3.0
